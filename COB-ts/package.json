{
  "name": "cob-calculator",
  "version": "0.1.0",
  "description": "Cost of borrowing / segmented mortgage amortization calculator engine",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "ui": "npm run build && node ui/serve.mjs"
  },
  "devDependencies": {
    "typescript": "^5.9.0",
    "vitest": "^2.1.0"
  }
}
